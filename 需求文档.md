# 大模型转发网关 — 需求文档

> 状态:需求已与用户确认(2025 讨论确认)
> 项目目录:当前工作区根目录(全新项目)

## 1. 项目定位

一个**大模型渠道管理 + OpenAI 兼容的透明转发网关**,单用户自用。
核心价值:

- 统一管理多家大模型渠道(baseurl + api key);
- 对外提供 OpenAI 兼容 API,客户端零改造直连;
- 通过"优先级 + 失败重试 + 降级 + 渠道冷静期"的路由策略,在多家渠道间智能分发(不做轮询,以最大化缓存命中);
- 记录请求与 token 用量,提供成本估算。

## 2. 已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 技术栈 | Go(后端)+ Vue3(前端)+ SQLite(数据库) |
| 对外协议 | OpenAI 兼容 API 网关(`/v1/*`),网关自行签发 API key |
| 用户体系 | 单用户自用:一个管理员账号 + 多把可撤销的网关 API key |
| 路由重试 | 失败先重试同一渠道一次,再按优先级降级到下一渠道 |
| 流式 | 一期必须支持 SSE 流式(末尾截获 usage 计入统计) |
| 上游协议 | 纯透传:只替换 baseurl 与 apikey,不做协议转换(渠道自身兼容 OpenAI 协议) |
| 冷静粒度 | 渠道整体冷静:任一模型失败 → 整个渠道冷静 10 分钟 |
| 用量统计 | 用量 + 成本估算:价格页列出所有模型,用户填价格,相同模型全局同价,仅粗略预测 |

## 3. 功能需求

### 3.1 渠道管理

- 添加/编辑/删除渠道,字段:
  - 渠道名称(name)
  - baseurl(含不含 `/v1` 由用户填写,按字符串拼接)
  - api key(鉴权凭据)
  - 可选:自定义鉴权头(默认 `Authorization: Bearer <key>`;部分渠道用 `x-api-key` 等)
  - 渠道全局优先级(数字越小越优先)
  - 启用/停用开关
- 渠道状态:正常 / 冷静中(含冷静截止时间);支持手动解除冷静、测试连通性。

### 3.2 模型管理

- **模型同步**:调用渠道 `${baseurl}/models` 拉取模型列表,勾选加入渠道;也可手动添加模型到渠道。
- **模型聚合**:模型页显示所有渠道模型的并集,同名(同模型 ID)合并只显示一个。
- 每个模型展示其可用渠道及按优先级排序的调用顺序。
- 可选:渠道关联模型时填"渠道内模型名",用于网关模型名 ≠ 渠道实际模型名的映射替换;留空则同名透传。

### 3.3 路由策略(核心差异化)

- 收到请求 → 找到所有含目标模型的**启用且非冷静**渠道 → 按渠道全局优先级排序 → 选第一个尝试。
- 请求失败 → **重试同一渠道一次**(透传重放请求体)→ 仍失败 → 按优先级逐个降级尝试下一个渠道 → 全部失败则向客户端返回最后一次错误。
- **冷静期**:渠道内任一模型请求失败(达到触发阈值,默认 1 次)→ 整个渠道冷静 10 分钟,期间不参与路由;到期自动恢复;可手动解除。触发阈值可配置。
- 失败判定:
  - 视为失败:连接错误 / 超时(默认 60s,可配)/ HTTP 5xx / HTTP 429;
  - 不视为渠道失败(业务错误,直接返回客户端):其余 4xx。

### 3.4 转发细节

- 网关对外路径 `/v1/*`,原样透传:转发目标 = `${baseurl}` + 客户端路径去掉 `/v1` 前缀。
  - 例:客户端 `POST /v1/chat/completions` → `${baseurl}/chat/completions`
  - 例:模型同步 `GET /v1/models` → `${baseurl}/models`
- 支持非流式与 SSE 流式:
  - 非流式:透传响应体,截获 `usage` 计入统计;
  - 流式:SSE 逐块透传,末尾 chunk 的 `usage` 计入统计。
- 请求体中的 `model` 字段默认原样透传;配置了"渠道内模型名"时替换后转发。

### 3.5 统计与用量

- 每请求记录一条日志:时间、渠道、模型、状态(成功/失败/超时/业务错误)、耗时、prompt/completion/total tokens、成本估算、错误信息。
- 聚合维度:按渠道、按模型、按时间(日/小时)聚合请求量、成功率、平均耗时、token 用量、成本。
- **价格**:价格页列出全局模型列表,用户填写价格(元/百万 token,输入/缓存读取/输出可分别填,不填则不计成本);相同模型全局同价。
- 成本估算公式:成本 = prompt_tokens × 输入单价 + completion_tokens × 输出单价(单价未填按 0)。

### 3.6 认证

- 管理后台:一个管理员账号(用户名 + 密码哈希),登录后访问管理 API 与前端。
- 网关 API:签发多把 API key(可命名、可撤销),客户端调用 `/v1/*` 时校验。

## 4. 非功能需求

- 部署:Windows 本机开发;Go 交叉编译 Linux 单二进制 + SQLite 单文件,一条命令启动;前端构建产物 embed 进二进制。
- 限流:一期不做 QPS 配额,只做简单并发控制。
- 数据:SQLite 单文件,定时/实时聚合统计(独立聚合表,避免长表全扫)。

## 5. 明确不做(一期范围外)

- 多用户/配额/余额体系;
- 非 OpenAI 兼容上游的协议转换(如 Anthropic 原生格式);
- 请求体语义处理(网关不解析、不修改业务内容,仅替换 baseurl/apikey/model 映射);
- 精确计费(成本仅为粗略估算)。
