# 大模型转发网关 — 开发计划

> 前置文档:需求文档.md
> 技术栈:Go(后端)+ Vue3 + Vite + Element Plus + ECharts(前端)+ SQLite(纯 Go 驱动)

## 1. 总体架构

```
┌─────────────┐  /v1/* + API key   ┌──────────────────────────┐
│  OpenAI 客户端 │ ───────────────────▶ │  网关(Go)                │
└─────────────┘                    │  ├─ 鉴权(API key)          │
                                   │  ├─ 路由决策(优先级/冷静期) │
                                   │  ├─ 透传代理(+重试/降级)    │
                                   │  ├─ 统计记录(日志/聚合)     │
                                   │  └─ 管理后台 API + Vue3 前端 │
                                   └──────────┬───────────────┘
                                              │ 替换 baseurl+apikey 原样转发
                              ┌───────────────┴───────────────┐
                              ▼              ▼               ▼
                       渠道A(baseurl+key)  渠道B           渠道C…
```

## 2. 数据模型(初步)

- **channels**:id、name、baseurl、api_key、auth_header(可选自定义鉴权头)、priority(越小越优先)、enabled、status(正常/冷静中)、cooldown_until(冷静截止时间)、failure_count(当前连续失败计数,触发冷静后重置)、created_at、updated_at
- **channel_models**:id、channel_id、model_id、upstream_model_name(渠道内模型名,可空=同名透传)、priority(模型级优先级,0=继承渠道全局优先级,拖拽自定义后为 1..N)、created_at;(唯一约束 channel_id + model_id)
- **models**:id、model_id(如 "gpt-4o")、display_name、price_input(元/百万 token,可空)、price_output(元/百万 token,可空)、price_cache_read(缓存读取价,元/百万 token,可空)、created_at、updated_at
- **api_keys**:id、name、key_hash(存哈希不存明文)、enabled、created_at、last_used_at
- **admin**:id、username、password_hash
- **request_logs**:id、request_time、channel_id、model、status(success/fail/biz_error)、latency_ms、prompt_tokens、completion_tokens、total_tokens、cache_read_tokens(命中缓存读取的 tokens)、cost、error、created_at
- **stat_daily** / **stat_hourly**:stat_date/stat_hour、channel_id、model_id、request_count、success_count、fail_count、biz_error_count、total_latency_ms、prompt_tokens、completion_tokens、total_tokens、cost;(唯一约束 时间 + channel + model)

说明:统计聚合表由 request_logs 增量写入时同步累加,避免查询时全表扫描。

## 3. Go 后端模块划分

```
cmd/gateway/            入口:启动 HTTP 服务、初始化 DB
internal/config/        配置加载(监听端口、管理员密码、冷静时长、超时等)
internal/model/         GORM/纯 SQL 数据模型定义
internal/store/         SQLite 存取层(渠道/模型/日志/统计 CRUD)
internal/server/
  ├─ admin/             管理后台 API(登录、渠道、模型、价格、API key、统计查询)
  ├─ proxy/             网关转发核心
  │   ├─ auth.go        API key 鉴权
  │   ├─ router.go      路由决策(优先级 + 冷静期 + 失败计数)
  │   ├─ forward.go     透传转发(非流式)
  │   ├─ stream.go      SSE 流式转发 + usage 截获
  │   └─ retry.go       重试同渠道一次 + 降级链
  └─ middleware/        日志、并发控制、恢复
internal/stat/          请求日志落库、统计聚合、成本计算
internal/sync/          渠道模型列表同步(/models 拉取)
web/                    Vue3 前端工程(构建产物 embed)
```

技术选型备注:
- HTTP:标准库 `net/http` + `http.ServeMux`(Go 1.22+ 支持方法路由),不引重型框架;
- SQLite:纯 Go 驱动 `modernc.org/sqlite`(免 CGO,交叉编译友好);
- 前端构建产物通过 `embed.FS` 打进二进制。

## 4. 路由与冷静期状态机

```
请求到达 /v1/* → 校验 API key → 解析目标 model
→ 候选渠道 = {含该模型 && enabled && 未冷静} 按 priority 升序
→ 取第一个渠道发送:
    ├─ 成功 → 记录日志/用量 → 返回
    ├─ 失败(连接/超时/5xx/429):
    │    ├─ 第 1 次:重试同一渠道(重放请求体)
    │    ├─ 仍失败 → 该渠道 failure_count+1,达到阈值(默认1)则进入冷静(cooldown_until = now + 10min)
    │    └─ 取下一个优先级渠道,重复上述流程
    └─ 业务错误(其余 4xx)→ 直接返回,不计渠道失败
→ 全部渠道失败 → 返回最后一次错误
冷静到期(定时器/惰性判断)→ 自动恢复;管理后台可手动解除
```

并发安全:候选渠道选择与状态更新由单写者(锁)保护;失败计数与冷静状态存内存 + 落库双份,重启后从库恢复冷静状态。

## 5. 分阶段实施计划

### 阶段 0 — 项目骨架
- [ ] Go 模块初始化、目录结构、配置加载、SQLite 连接与迁移(建表)
- [ ] Vue3 + Vite + Element Plus 工程初始化
- [ ] 构建脚本:前端 `vite build` → Go `embed` → 单二进制;`go build`(Windows)与交叉编译 Linux
- 验收:`go build` 通过,启动后访问返回内置前端页面;DB 文件自动创建

### 阶段 1 — 渠道与模型管理
- [ ] 管理员登录(单账号,密码哈希)
- [ ] 渠道 CRUD 管理 API(含自定义鉴权头、优先级、启停)
- [ ] 模型同步:调渠道 `${baseurl}/models` 拉取列表(测试连通性 API)
- [ ] 渠道-模型关联(手动添加 / 同步勾选,支持"渠道内模型名"映射字段)
- 验收:可通过 API 完成 添加渠道 → 同步模型 → 勾选关联

### 阶段 2 — 转发核心(网关可用)
- [ ] API key 签发/撤销/鉴权中间件
- [ ] 非流式透传代理(替换 baseurl/apikey,可选 model 映射)
- [ ] SSE 流式转发 + 末尾 chunk usage 截获
- [ ] 路由决策:优先级排序、冷静期、连续失败计数
- [ ] 重试同一渠道一次 + 降级链;超时配置
- 验收:用标准 OpenAI 客户端(如 curl/Chatbox)走网关请求真实渠道,非流式与流式均正常;构造失败渠道验证降级与冷静

### 阶段 3 — 统计与用量
- [ ] 请求日志落库(含 token/成本/耗时/状态)
- [ ] 统计聚合表(日/小时 × 渠道 × 模型)增量累加
- [ ] 价格配置(全局模型价格,输入/输出单价)
- [ ] 成本估算计算与统计查询 API
- 验收:压测一批请求后,统计 API 返回正确的请求量/成功率/用量/成本

### 阶段 4 — 前端界面
- [ ] 登录页
- [ ] 渠道管理页(列表/编辑/测试连通/手动解除冷静)
- [ ] 模型聚合页(并集去重、渠道顺序展示、同步/勾选管理)
- [ ] 价格页(全局模型价格填写)
- [ ] 统计看板(ECharts:请求量/成功率/用量/成本,按日/小时、渠道/模型筛选)
- [ ] API key 管理页
- 验收:管理界面全流程可用,构建产物正常嵌入二进制

### 阶段 5 — 打磨与交付
- [ ] 单元测试:路由决策、冷静期、成本计算(重点)
- [ ] 集成测试:代理转发(用 httptest 模拟渠道)
- [ ] 错误处理与日志完善、并发控制压测
- [ ] 部署文档(单二进制 + SQLite 启动、交叉编译 Linux 说明)
- 验收:测试通过,交付文档齐全

## 6. 风险与应对

| 风险 | 应对 |
|---|---|
| 流式 usage 截获:部分渠道流末尾无 usage | 统计兜底为 0,不阻塞转发;支持按渠道标记"无用量" |
| 冷静期内存态重启丢失 | 冷静状态落库,启动时恢复 |
| 重试导致重复计费(上游已计费但响应丢失) | 一期接受(透传网关无法避免),文档注明;后续可做可选重试开关 |
| 渠道 baseurl 形态不统一(含/不含 /v1) | 明确拼接规则:客户端路径去 `/v1` 前缀 + baseurl 原样拼接;渠道页提示示例 |
